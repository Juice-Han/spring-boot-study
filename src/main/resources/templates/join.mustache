<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Join Page</title>
</head>
<body>
{{>navbar-default}}
<div class="container mt-5 mt-5 p-4 border border-2 rounded" style="max-width: 500px">
    <h3>회원가입</h3>
    {{#duplicateId}}
        <div class="alert alert-danger" role="alert">
            아이디가 중복되었습니다. 다른 아이디로 시도해주세요.
        </div>
    {{/duplicateId}}
    <form id=joinForm class="mt-3" action="/joinProc" method="post" name="joinForm">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <div class="mb-3">
            {{#joinInfo}}
                <label for="username" class="form-label">아이디</label>
                <input class="form-control" id="username" type="text" name="username" placeholder="id"
                       value="{{joinInfo.username}}"/>
                {{#validMessage_username}}
                    <div class="form-text text-danger">{{validMessage_username}}</div>
                {{/validMessage_username}}
                <label for="password" class="form-label">비밀번호</label>
                <input class="form-control" id="password" type="password" name="password" placeholder="password"
                       value="{{joinInfo.password}}"/>
                {{#validMessage_password}}
                    <div class="form-text text-danger">{{validMessage_password}}</div>
                {{/validMessage_password}}
            {{/joinInfo}}
            {{^joinInfo}}
                <label for="username" class="form-label">아이디</label>
                <input class="form-control" id="username" type="text" name="username" placeholder="id"/>
                <label for="password" class="form-label">비밀번호</label>
                <input class="form-control" id="password" type="password" name="password" placeholder="password"/>
            {{/joinInfo}}
        </div>
    </form>
    <div class="text-end">
        <input class="btn btn-primary" form="joinForm" type="submit" value="등록"/>
        <button class="btn btn-secondary" onclick="location.href='/login'">로그인 창으로</button>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>
</html>